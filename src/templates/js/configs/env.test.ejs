APP_URL=
NODE_ENV=development
APP_PORT=

# Auth

APP_SECRET=mysecret

# Database

DB_DIALECT=sqlite
DB_HOST=localhost
DB_USER=docker
DB_PASS=docker
DB_NAME=tests
DB_STORAGE=./__tests__/database.sqlite